# Ops: Sync / Seed / Cron — Feature Doc

## Summary
Operational scripts to seed defaults and sync state (cron jobs, sessions, memories) into Convex for UI display.

## How to use
- Seed: `npm run seed:defaults`
- Sync all: `npm run sync:all`
